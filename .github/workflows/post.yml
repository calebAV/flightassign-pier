name: Post pier view to Slack

on:
  # Triggered externally (e.g., by cron-job.org) via GitHub's workflow_dispatch
  # API for precise 20-min cadence. GitHub's own cron scheduler is intentionally
  # not used here because it drifts 0-15 min per run.
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Render to logs only (no Slack post)?"
        required: false
        default: "false"

concurrency:
  group: post-pier-view
  cancel-in-progress: false

jobs:
  post:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies and package
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -e .

      - name: Post pier view
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL: ${{ secrets.SLACK_CHANNEL }}
          FLEET_API_BASE: ${{ vars.FLEET_API_BASE }}
          AIRPORT: ${{ vars.AIRPORT }}
          HOURS_FORWARD: ${{ vars.HOURS_FORWARD }}
          IN_SCOPE_GATES: ${{ vars.IN_SCOPE_GATES }}
          PIER_MIN: ${{ vars.PIER_MIN }}
          PIER_MAX: ${{ vars.PIER_MAX }}
          HAULOUT_LEAD_MIN: ${{ vars.HAULOUT_LEAD_MIN }}
          DISPLAY_TZ: ${{ vars.DISPLAY_TZ }}
        run: |
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            python -m flightassign_pier --dry-run --verbose
          else
            python -m flightassign_pier --verbose
          fi
